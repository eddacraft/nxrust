{
  "$schema": "http://json-schema.org/schema",
  "version": 2,
  "outputCapture": "direct-nodejs",
  "title": "@eddacraft/nxrust:fmt",
  "description": "Format a Rust crate with `cargo fmt`.",
  "type": "object",
  "properties": {
    "toolchain": { "type": "string", "default": "stable" },
    "package": { "type": "string" },
    "check": {
      "type": "boolean",
      "default": false,
      "description": "Run in check mode; exit non-zero on any formatting diff."
    },
    "all": {
      "type": "boolean",
      "default": false,
      "description": "Format every workspace member (overrides the per-project package filter)."
    },
    "args": {
      "oneOf": [
        { "type": "string" },
        { "type": "array", "items": { "type": "string" } }
      ],
      "description": "Raw passthrough to `rustfmt` (use to supply --edition, --config, etc.)."
    }
  },
  "required": []
}
