[package]
name = "<%= cargoName %>"
version = "0.1.0"
edition = "<%= edition %>"<% if (description) { %>
description = "<%= description %>"<% } %>

[lib]
name = "<%= libName %>"

[dependencies]

[dev-dependencies]

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html
