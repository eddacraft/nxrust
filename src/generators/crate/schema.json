{
  "$schema": "http://json-schema.org/schema",
  "$id": "NxRustCrate",
  "title": "nxrust:crate",
  "description": "Scaffold a new Cargo workspace-member crate.",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Crate name (cargo and nx project name).",
      "$default": { "$source": "argv", "index": 0 },
      "x-prompt": "What name would you like to use for the crate?"
    },
    "directory": {
      "type": "string",
      "description": "Parent directory under the workspace root. Defaults to `crates`.",
      "default": "crates"
    },
    "bin": {
      "type": "boolean",
      "default": false,
      "description": "Generate a binary crate (with src/main.rs) instead of a library."
    },
    "edition": {
      "type": "string",
      "enum": ["2015", "2018", "2021", "2024"],
      "default": "2021"
    },
    "description": {
      "type": "string",
      "description": "Short description written to the crate's Cargo.toml."
    },
    "tags": {
      "type": "string",
      "description": "Comma-separated Nx project tags."
    },
    "skipFormat": {
      "type": "boolean",
      "default": false
    }
  },
  "required": ["name"]
}
